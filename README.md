# COA-ALU-logisim
A 4-bit Arithmetic Logic Unit (ALU) designed in Logisim for Computer Organization and Architecture course project. Supports arithmetic operations (addition, subtraction), logic operations (AND, OR, XOR, NOT), and shift operations (left/right shift). Includes detailed project report and presentation.
